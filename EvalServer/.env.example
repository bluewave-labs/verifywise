# EvalServer Environment Variables
# 
# For local development, you can simply copy the root .env.dev file:
#   cp ../.env.dev .env
#
# Or create a minimal .env with just these required variables:

# Database Configuration (required)
DB_USER=postgres
DB_PASSWORD=test
DB_HOST=localhost
DB_PORT=5432
DB_NAME=verifywise

# Backend URL for CORS (optional, defaults to http://localhost:3000)
BACKEND_URL=http://localhost:3000

# Redis URL (optional, defaults to redis://localhost:6379/0)
REDIS_URL=redis://localhost:6379/0
