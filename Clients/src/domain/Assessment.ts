

export interface AssessmentProgress {
  // Define the structure of your assessment progress data here
  // For example:
  totalQuestions: number;
  answeredQuestions: number;
}
export interface AssessmentData {
  id: string;
  is_demo: boolean;
  project_id: string;
}