export enum ApprovalStatus {
    APPROVED = "Approved",
    REJECTED = "Rejected",
    PENDING = "Pending",
}

export enum ApprovalStepStatus {
    Completed = "completed",
    Pending = "pending",
}
